---
name: Upgrade Static Files (CSS/JS)

on:
  workflow_dispatch:
  schedule:
    # Runs every Monday at 8:00 UTC
    - cron: '0 8 * * *'

permissions:
  contents: write
  pull-requests: write

jobs:
  upgrade:
    uses: DLRSP/workflows/.github/workflows/upgrade-static-files.yaml@main
    secrets: inherit
